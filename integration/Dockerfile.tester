FROM archlinux/base

RUN pacman -Syu --noconfirm python-pip docker npm jdk8-openjdk git
RUN pip install httpie
RUN npm install @openapitools/openapi-generator-cli -g

ENV PATH "/root/.cache/pip:$PATH"
